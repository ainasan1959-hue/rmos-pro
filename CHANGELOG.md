# RMOS Pro 変更履歴

機能を追加・修正するたびに、ここに1行〜数行で記録する。
「いつから」「何が」変わったかが分かれば、不具合が起きたときに原因を探す時間が大幅に減る。

## 書き方のルール
- 新しい変更を一番上に追加する
- 日付は西暦/月/日
- 「追加」「修正」「削除」のどれかを最初に書く
- index.html と GAS（Apps Script）のどちらを変更したかを書く

## 2026-09-13（続き）
- **修正**（GAS）：`getMyAnswers` の各件に `row`（スプレッドシートの行番号）を追加。住民側で個別の履歴を特定できるようにするため
- **修正**（index.html）：個別回答画面に、履歴ごとの「✕ 非表示にする」ボタンを追加
  - タップすると確認ダイアログのうえ、その履歴を**この端末のブラウザ内だけ**で非表示にする（`localStorage`に号室ごとの非表示リストを保存）
  - 管理組合側のスプレッドシートの記録は削除されない。別の端末や、ブラウザのデータを消去した場合は再び表示される

## 2026-09-13
- **修正**（GAS）：掲示板用カレンダー（`getMonthlyUsageCalendar`）にキャッシュを追加
  - シートの全行スキャンは件数が増えるほど遅くなるため、月ごとの集計結果を`CacheService`で5分間キャッシュ。同じ月への再アクセスはシートを読まずに即座に返す
  - 承認が発生した月（駐車場は開始〜終了が月をまたぐ場合は両方の月、集会室は使用日の月）は、新設の`invalidateUsageCalCache_()`でキャッシュを明示的に削除し、古い集計が表示され続けないようにした（`applyParkingApproval_`・`decideMeetingRequest`承認時から呼び出し）

## 2026-09-11（続き6）
- **修正**（GAS）：駐車場・集会室の利用集計を自動生成
  - 新規シート「駐車場_利用集計」：区画マスタの全区画を行として、区画番号・種別・利用回数・使用料合計・直近利用日・直近利用者（号室・氏名）を集計。未使用の区画も0件で表示
  - 新規シート「集会室_利用集計」：集会室は1室のみのため、利用回数・使用料合計・直近利用日・直近利用者を1行に集計
  - どちらも承認されるたびに`updateParkingUsageSummary_()`／`updateMeetingUsageSummary_()`が自動で全体を再計算するため、理事会が手動で集計・更新する必要はない

## 2026-09-11（続き5）
- **修正**（index.html）：集会室フォームの日時入力時に出る警告文が、旧仕様（「時間の重複は理事会にて調整しますので、そのまま申請いただいて構いません」）のままになっていたのを、現在の自動ブロック仕様に合わせて「この時間帯では申請できませんので、別の日時でお願いします」に修正

## 2026-09-11（続き4）
- **修正**（GAS）：駐車場・集会室ともダブルブッキング対策を追加
  - 集会室：`meetingRoomRequest()` に新規ヘルパー `checkMeetingConflict_()` を追加し、同じ使用日で時間が重なる既存申請（却下・重複エラーを除く）があれば「重複エラー」として自動ブロックするよう変更。申請成功時のみ返していたレスポンスをJSON化し（`result:'ok'`／`result:'conflict'`）、重複時は別日時を促すメッセージを返す
  - 駐車場・集会室とも、ほぼ同時に2件送信された場合の競合（レースコンディション）を防ぐため、重複チェックとシートへの書き込みを `LockService.getScriptLock()` で排他制御する区間に変更
  - 申請承認パネル（駐車場・集会室）の一覧取得フィルタに「重複エラー」を追加し、⚠アイコン付きで理事会が確認できるようにした
- **修正**（index.html）：`submitMeeting()` の通信を `mode:'no-cors'` → `mode:'cors'` に変更し、GASからの`conflict`応答を受け取って画面にエラーメッセージを表示できるようにした（駐車場と同じ挙動）

## 2026-09-11（続き3）
- **修正**（GAS）：承認時の個別回答メッセージに、使用料の入金依頼を追加
  - `applyParkingApproval_` に `fee` 引数を追加し、駐車場承認時の個別回答に「使用料◯◯円を…お支払いください」の一文を追加（Pro経由は後払い可の旨も併記）
  - `decideMeetingRequest` の承認時も同様に、使用料を個別回答メッセージに含めるよう修正

## 2026-09-11（続き2）
- **修正**（GAS）：集会室の使用日・開始終了時刻がカレンダーに反映されない不具合を修正
  - 原因：スプレッドシートが "2026-09-15" や "10:00" のような文字列を自動的に日付／時刻型セルへ変換することがあり、その場合の文字列比較・文字列連結が壊れていた（例：`date + 'T' + startTm + ':00'` が日付オブジェクトの曜日付き文字列と連結され不正な値になる）
  - 対策1（書き込み時）：`meetingRoomRequest()` で使用日・開始時刻・終了時刻のセルを明示的に「書式なしテキスト」に固定し、自動変換自体を防止
  - 対策2（読み込み時）：新設の `formatSheetDateOnly_` / `formatSheetTimeOnly_` を、`getMeetingBookings`・`getMeetingRequests`・`getMonthlyUsageCalendar`・`decideMeetingRequest` の該当箇所すべてに適用し、既存データが日付／時刻型セルになっていても正しく読めるようにした
- **修正**（index.html）：駐車場・集会室とも、申請成功後に送信ボタンが無効化されたまま戻らず、画面をリロードしないと連続して申請できない不具合を修正。成功メッセージ表示後、2秒後にボタンを再度押せる状態に戻すよう変更

## 2026-09-11（続き）
- **修正**（GAS）：駐車場・集会室の申請結果を「個別回答」で通知
  - 新規ヘルパー `postIndividualReply_(room, category, content, answer)`：意見箱シートに、公開区分「個別」・回答方法「個別回答」として1行追加する。既存の意見箱の個別回答の仕組み（`getMyAnswers`・号室一致で表示）にそのまま乗る
  - `applyParkingApproval_`・`decideParkingRequest`（却下時）・`decideMeetingRequest`（承認・却下とも）から呼び出し、申請者が「個別回答」画面で号室を入力すれば結果を確認できるように変更（全体向けプッシュ通知は従来どおり継続）
- **修正**（GAS）：`getMonthlyUsageCalendar` の返却データを拡張
  - 駐車場：各日のエントリに区画番号（`lot`）を追加
  - 集会室：氏名が「理事会」のときだけ `name` を含める（それ以外は号室・氏名を出さない方針を維持）
- **修正**（index.html）：`showUsageDay`（カレンダーの日付タップ時の詳細表示）で、駐車場は区画番号、集会室は「理事会」名義のときだけ名称を併記するよう更新

## 2026-09-11
- **修正**（GAS・index.html）：掲示板用の利用状況カレンダーを機能強化
  - `getMonthlyUsageCalendar`：戻り値を「予約がある日の一覧（true/false）」から「日付ごとの種別（駐車場／集会室）と時間帯の配列」に変更。号室・氏名・区画・目的などは引き続き含めない
  - カレンダーに前月・翌月の矢印ナビゲーションを追加（`renderUsageCalendar(-1)`/`renderUsageCalendar(1)`）
  - 日付セルを駐車場＝橙・集会室＝青で色分けし、該当するアイコン（🅿️／🏛）を表示。両方ある日は斜め2色
  - 日付をタップすると、その日の予約時間帯（開始〜終了）を種別ごとに一覧表示する詳細パネルを追加（`showUsageDay`）

---

## 2026-09-10
- **修正**（index.html）：駐車場一時使用申請フォームを復活。駐車場使用細則案（修正版9）第7章（来客用一時使用）の運用ルールに合わせて再設計
  - フォーム項目・区画一覧取得（renderParking）・料金計算（calcParkingFee）・送信（submitParking）・承認パネル連携（getParkingRequests/decideParkingRequest）自体はコード上に残っていたため、無効化されていたHTML部分（「🚧現在調整中です」案内文）のみを差し替え。showSec内で止めていたrenderParking()呼び出しも復活
  - ユーザー指示により、駐車場・集会室とも申請フォームの項目順を「日時選択を最優先」に変更（使用開始日時・終了日時／使用日・時刻を先頭に配置し、号室・氏名等の入力はその後）
  - 第19条第1項（使用開始48時間前までの申込み、空き区画がある場合の例外）に対応する注意書きを追加（checkParkingLeadTime関数を新設、48時間未満の場合に警告表示。区画の空き状況自体はCSVマスタの現況ベースのままで、日時範囲による自動的な空き判定は未対応）
  - 承認パネル（申請承認タブ）側の「🚧申請フォームを調整中のため…」の固定案内文を削除し、既存の一覧表示ロジックに戻した
- 未対応事項：細則案第18条の2「登録区画」（契約者が任意で自分の契約区画を一時貸出に登録し、使用料を按分受取る仕組み）は今回未実装。将来対応時はGAS側にも区画ごとの登録状態・払戻し管理の追加が必要

## 2026-09-10（続き）
- **修正**（GAS）：駐車場一時使用申請を自動登録方式に変更
  - `parkingRequest()`：申請時に `checkParkingConflict_()` で同一区画・日時の重なりを判定。重なりが無ければその場で「承認」状態にし、カレンダー登録・お知らせ掲示・プッシュ通知まで自動実行（`applyParkingApproval_()` として承認処理を共通ヘルパー化し、手動承認の `decideParkingRequest()` とも共有）。重なりがあれば「重複エラー」状態で記録し、メールで管理者に通知
  - 「駐車場_区画マスタ」のステータス書き換えを`parkingRequest()`から削除：従来は申請のたびに該当区画を無条件で「使用中」にしており、却下しても「空き」に戻す処理が無かったため、一度申請が入ると区画が永久に埋まったままになる不具合があった。マスタのステータスは「長期契約で埋まっているか」専用の意味に戻した
  - 新規API `getParkingAvailability`：指定した開始〜終了日時と重なる（却下・重複エラーを除く）申請がある区画IDの一覧を返す。フロント側で該当区画を選択肢から除外はせず「使用予定あり」表示のうえ選択不可にする（ブロック方式）
  - 新規API `getMonthlyUsageCalendar`：「駐車場_申請」「集会室_申請」の承認済みレコードから、指定した年月のうち予約がある日付一覧を返す（号室・区画等の詳細は含めない簡易版）
  - `getParkingRequests`のフィルタ先（フロント側）を「受付済」→「重複エラー」に変更。通常は自動承認されるため、申請承認パネルの駐車場欄には重複エラーのみが表示される
- **修正**（index.html）：
  - 駐車場フォームに日時入力後の空き区画チェックを追加（`checkParkingAvailability()`）。使用予定がある区画は一覧に残しつつ選択不可＋「（使用予定あり）」表示に
  - `submitParking()` の通信を `mode:'no-cors'` → `mode:'cors'` に変更し、GASからの`conflict`応答を受け取って画面に表示できるようにした
  - 申請承認パネルの案内文・空欄時メッセージ・駐車場欄の見出しを「重複エラーのみ表示」の運用に合わせて更新
  - 駐車場・集会室申請ページの先頭に、居住者向け公開エリア用の簡易月間カレンダー（`renderUsageCalendar()`）を追加。予約がある日だけ色付けする粗い表示（号室等の詳細は出さない）
  - 駐車場申請の案内文を、自動登録・前払い原則＋Pro経由は後払い可、という運用に合わせて更新

## 2026-08-05
- 修正：最終製本版（令和8年7月19日制定）に合わせて data.js の HOSOKU_DATA（細則）を全面再生成
  - 旧「第9編 理事会運営細則」→ 最終版「第8編 理事会運営細則」（第4条の2〜第4条の7の新規条文6本を追加）
  - 旧「第5編 契約及び業者選定細則」→ 最終版「第5編 契約及び業者選定に関する細則」（編名修正）
  - 旧「第8編 駐車場使用細則」（抽選・更新・解約等の全18条）は最終製本版に収録なし。修繕・管理検討委員会で今後検討予定のため、Pro上のデータからも削除
  - KIYAKU_DATA（規約）は既に最終版と一致していたため変更なし
- 修正：index.html の RMOS_FAQ（届出・申請ガイドブック等）の条文参照（refs）を新しい条番号・編名に合わせて再マッピング（85件）
  - 駐車場の新規申込み・更新解約・抽選等を扱うFAQ 8問（12参照）は、根拠となる細則が最終版に存在しないため要対応（本文は未修正のまま）
- 修正：manual.html / マニュアル.html の「細則（9編）」表記を「細則（8編）」に修正

## 2026-07-15

- **追加**（index.html）：公開エリアのFAQに「届出・申請の早見表」カテゴリを新規追加（アップロードされた「届出・申請ガイドブック」をQ&A形式に変換）。全14問、各回答に該当する規約・細則条文へのジャンプリンク付き。一覧の一番上に表示される
  - 参考：規約・細則の条文番号は令和8年7月19日施行の改正規約と既に一致しているため、総会後の番号修正は不要（ガイドブック作成時点で新番号を使用済み）
- **追加**（GAS）：意見箱で「個別」回答を公開したときの通知を追加。①誰宛かを明かさない汎用文面（「意見箱に個別回答が届いています」）でプッシュ通知を全体に送信、②連絡先にメールアドレスが登録されていれば、質問と回答をそのまま本人へメール送信（電話番号のみの場合はメール送信をスキップ）

---

## 2026-07-14

- **追加**（index.html / GAS）：意見箱の投稿フォームで、回答方法に「個別回答・電話・書面・訪問」を選んだ場合、部屋番号かメール／電話番号のどちらかの入力を必須化
- **修正**（GAS）：`publishAnswer` が I列（公開／個別）に書き込めなかった不具合を修正。原因はスプレッドシートのデータ入力規則が「個別」という値を許可していなかったため。書き込み直前に `clearDataValidations()` で規則を解除する形に変更
- **修正**（GAS）：意見箱・個別回答・システム確認パネルの日時表示が最大16時間ずれる不具合を修正。原因はスプレッドシートが日時の文字列を自動で日付型セルに変換し、ファイルのタイムゾーン設定とのズレが生じていたため。保存時に「書式なしテキスト」で固定する形に変更
- **修正**（index.html）：意見箱の回答送信ボタンが `no-cors` 通信を使っていたため、サーバー側で失敗していても画面上は「成功」と表示されてしまう不具合を修正。`cors` 通信に変更し、実際の成否を画面に表示するように変更
- **追加**（GAS）：`getMyAnswers`（個別回答の取得）・`getSystemNotices`（システムテスト投稿の確認）を実装。あわせて号室の全角・半角ゆれを吸収する `normalizeRoom_` を追加
- **追加**（index.html / GAS）：緊急プッシュ通知パネルからの送信を、Web PushとLINE公式アカウント（@118ketww）に同時配信するよう変更（GAS経由に統一し、チャネルアクセストークンをブラウザ側に置かないようにするため）
- **追加**（index.html / GAS）：通常のお知らせ投稿にも「LINEにも配信する」チェックボックスを追加（緊急・重要なときだけ管理者が都度選ぶ運用、無料枠を消費するためデフォルトはOFF）
- **追加**（GAS）：岡崎市防災メール（mail@city.okazakibosai.jp）の自動取込み機能（`checkBosaiMail`）。重大なもの（避難指示・特別警報等）のみWeb Push・LINE配信、それ以外はお知らせに記録のみ
- **追加**（index.html）：「システム（管理者テスト用）」配信先。選択すると投稿ログにのみ記録され、実際の配信は一切行われない（バグ修正時の動作確認用）
- **修正**（index.html）：お知らせの添付リンクを複数対応に変更（カンマ区切りで複数URL、2件以上は「📎資料1」「📎資料2」…とボタン表示）
- **修正**（index.html）：CSV解析処理が、本文が複数行のお知らせで列がズレる不具合を修正
- **修正**（index.html）：お知らせの「エリアを超えて配信される」不具合の一部原因だった、カレンダー予定の内容表示（直近の予定欄）を廃止し、カレンダーへのリンクのみに変更

