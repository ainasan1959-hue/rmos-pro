# 電工日報 Phase1 PoC セットアップ手順

## 全体構成
- 職人さん用の入力画面：`index.html`（スマホのブラウザで開く。GitHub Pagesなどに置く想定）
- データの保存先：Googleスプレッドシート
- 橋渡し役：Google Apps Script（GAS）

## 手順

### 1. スプレッドシートを作成する
稲垣さんのGoogleアカウントで新規スプレッドシートを1つ作成し、シートを3枚用意します。

**シート「現場マスタ」**（1行目は見出し）

| A: 現場ID | B: 現場名 | C: 現在工程番号 | D: 現在工程名 | E: 判定 | F: 最終更新日時 | G: 最終報告者 |
|---|---|---|---|---|---|---|
| S001 | 岡崎○○様邸 新築工事 | | | | | |
| S002 | ○○ビル 改修工事 | | | | | |

→ 今動いている現場を4〜10件、A・B列だけ手入力しておいてください（現場IDは自由な文字列でOK）。C〜G列は日報が送られると自動で埋まります。

**シート「工程マスタ」**（1行目は見出し）

現場ごとに、工程を工程番号順で好きなだけ入力します（現場によって工程数が違ってもOKです）。

| A: 現場ID | B: 工程番号 | C: 工程名 | D: 予定日 |
|---|---|---|---|
| S001 | 1 | 着工 | 2026-09-10 |
| S001 | 2 | 配線 | 2026-09-18 |
| S001 | 3 | 検査 | 2026-09-25 |
| S001 | 4 | 完了 | 2026-09-30 |
| S002 | 1 | 着工 | 2026-09-15 |
| S002 | 2 | 検査 | 2026-09-22 |
| S002 | 3 | 完了 | 2026-09-28 |

→ 工程名は現場の種類に合わせて自由に変えて構いません（「着工・配線・検査・完了」は一例です）。

**シート「日報ログ」**（1行目は見出しだけでOK、以降は自動で追記されます）

| A: 日時 | B: 職人名 | C: 現場ID | D: 現場名 | E: 工程番号 | F: 工程名 | G: 作業内容 | H: 写真URL |
|---|---|---|---|---|---|---|---|

### 2. Apps Scriptを設定する
1. スプレッドシート（KBL_進捗管理表、ID: 19K0ivlN2BZFk4yrV_tkCB6gLWfitJRpuWMsOUWYla_A）のメニューから「拡張機能」→「Apps Script」を開く
2. デフォルトのコードを全部消して、`gas-code.gs.txt` の中身を貼り付ける
3. `SPREADSHEET_ID` はこのスプレッドシートのIDに設定済みです
4. `NOTIFY_EMAIL` は kawai582000@yahoo.co.jp に設定済みです。日報が送信されるたびに、このアドレスへ確認メールが届きます（現場・職人・工程・判定・作業内容を記載）
5. 写真を使う場合は、Googleドライブに保存用フォルダを1つ作り、そのフォルダIDを `DRIVE_FOLDER_ID` に書き換える（写真を使わない場合はそのままでOK。写真は無視されます）

### 3. ウェブアプリとして公開する
1. Apps Scriptの画面右上「デプロイ」→「新しいデプロイ」
2. 種類の選択で「ウェブアプリ」を選ぶ
3. 「次のユーザーとして実行」：自分
4. 「アクセスできるユーザー」：全員
5. 「デプロイ」を押し、表示された **ウェブアプリのURL** をコピーしておく

### 4. index.htmlを設定する
`index.html` を開いて、以下の行を書き換えます。

```js
const GAS_URL = "https://script.google.com/macros/s/★ここにデプロイ後のURLを貼る★/exec";
```

職人さんの名前は「職人マスタ」シートから自動取得されるので、index.html自体を書き換える必要はありません（後述のPhase2セクションにある「職人マスタ」シートに名前を入力してください）。

### 5. 公開する
- 一番手軽なのは、これまでのRMOS Proと同じくGitHub Pagesにアップロードする方法です
- 職人さんのスマホでURLを開き、「ホーム画面に追加」しておけばアプリのように使えます

### 6. 動作確認
1. スマホでURLを開く
2. 名前を選ぶ→現場を選ぶ（選ぶと工程ボタンが自動で表示される）→工程を選ぶ→作業内容を入れて送信
3. スプレッドシートの「日報ログ」に1行増えていること、「現場マスタ」の該当行（現在工程番号・現在工程名・判定）が更新されていることを確認
4. 工程マスタの予定日を今日より前の日付にしてから遅い工程番号で報告してみて、「判定」が「遅れ」になることも確認してみてください

## 遅れ・順調の判定ロジック
- 工程マスタの「予定日」のうち、今日以前になっている工程の中で一番大きい工程番号を「本来ここまで進んでいるはずの工程番号」とする
- 職人さんが報告した工程番号がそれ以上なら「順調」、それより手前なら「遅れ」

## Phase1でできないこと（次のフェーズで対応）
- 会社側からのプッシュ通知による作業指示配信 → **Phase2**
- 「現場の進捗状況」一覧（遅れ／順調バッジ付き）を会社側が見られるダッシュボード画面 → **Phase3（admin.htmlとして完成済み）**

---

# Phase2：プッシュ通知の設定

RMOS Proとは別に、KBL専用のCloudflare Worker・VAPID鍵を新規に用意しています。

## 生成済みの鍵・合言葉（すでに各ファイルに設定済みです）

- VAPID公開鍵：`BAXKJwcvLvHDgNmajXGtnaDXA0Nf2I5BfOdMA4CVCXCOZfL9xSs9HSG2I-IL9WHp2G08aJKkj-NwhhDXRwyKjRk`
- VAPID秘密鍵：`Z6qufx9VIXUMbP6zJuboNoGxp9Nfl15VLIAR-K8OIT8`（Cloudflare Worker側にのみ設定。他には出さないでください）
- NOTIFY_SECRET：`db37508c5eaabca1e9a6b2104e35ba03`

## 1. スプレッドシートに「指示ログ」と「職人マスタ」シートを追加

**シート「指示ログ」**（1行目は見出しだけでOK、以降は自動で追記される）

| A: 日時 | B: 現場ID | C: 現場名 | D: 対象職人 | E: 指示内容 |
|---|---|---|---|---|

**シート「職人マスタ」**（1行目は見出し、2行目以降に職人名を1人1行で入力）

| A: 職人名 |
|---|
| 山田 |
| 鈴木 |
| 佐藤 |

→ index.html・admin.htmlの両方が、この「職人マスタ」から名前を自動取得するようになりました。職人の増減はこのシートを編集するだけでOKです（ファイルの書き換えは不要）。

## 2. Cloudflare Workerを作成する

1. Cloudflareダッシュボード（https://dash.cloudflare.com）にログイン（アカウントがなければ無料で作成）
2. 「Workers & Pages」→「Create」→「Create Worker」
3. 名前は例えば `kbl-push` などにして作成
4. エディタが開いたら、デフォルトのコードを全部消して `worker.js` の中身を貼り付けてデプロイ
5. Worker の「設定」→「変数とシークレット」で以下を追加
   - `NOTIFY_SECRET` = `db37508c5eaabca1e9a6b2104e35ba03`（シークレットとして追加）
   - `VAPID_PUBLIC_KEY` = `BAXKJwcvLvHDgNmajXGtnaDXA0Nf2I5BfOdMA4CVCXCOZfL9xSs9HSG2I-IL9WHp2G08aJKkj-NwhhDXRwyKjRk`
   - `VAPID_PRIVATE_KEY` = `Z6qufx9VIXUMbP6zJuboNoGxp9Nfl15VLIAR-K8OIT8`（シークレットとして追加）
   - `VAPID_SUBJECT` = `mailto:ご自身のメールアドレス`
6. 「KV Namespace」を1つ新規作成（例：`KBL_PUSH_SUBSCRIPTIONS`）し、このWorkerの「設定」→「変数とシークレット」の「KV Namespace Bindings」で、変数名 `KBL_PUSH_SUBSCRIPTIONS` としてこのNamespaceを紐付ける
7. 保存・再デプロイすると、WorkerのURL（例：`https://kbl-push.あなたのアカウント名.workers.dev`）が発行されます

## 3. WorkerのURLを各ファイルに設定する

- `index.html` の `PUSH_WORKER_URL`
- `admin.html`（もしプッシュ関連の機能を追加した場合）
- `gas-code.gs.txt` の `PUSH_WORKER_URL`

の3か所を、発行されたWorkerのURLに書き換えてください（末尾のスラッシュは付けない）。

## 4. 動作確認

1. スマホでindex.htmlを開き、名前を選ぶと通知の許可ポップアップが出るので「許可」する
2. admin.htmlの「指示配信」タブから、現場・対象（全員 or 個人）・指示内容を入力して配信
3. スマホ側に通知が届けば成功です

## 補足
- 職人の増減は「職人マスタ」シートを編集するだけでOKです（index.html・admin.htmlの書き換えは不要）

---

# Phase3-α：計画・実績カレンダー連携

工程マスタの予定日を「KBL 計画」カレンダーに、日報の実績を「KBL 実績」カレンダーに、それぞれ自動反映させる機能です。

## 1. 工程マスタにE列を追加

「工程マスタ」シートのE1セルに見出し「カレンダーイベントID」を追加してください（中身は空でOK、自動で埋まっていきます）。

## 2. カレンダーを作成する

1. Apps Scriptエディタで、関数選択のプルダウンから `setupCalendars` を選び、実行（▷）ボタンを押す
2. 初回は権限の承認を求められるので許可する
3. 実行後、下部の「実行ログ」に「計画カレンダーID」「実績カレンダーID」が表示されるので、それぞれコピーする
4. コード冒頭の `PLAN_CALENDAR_ID` と `ACTUAL_CALENDAR_ID` に、コピーしたIDを貼り付けて保存・再デプロイする

## 3. 自動同期トリガーを設定する

1. 関数選択のプルダウンから `installTriggers` を選び、実行する（これも初回のみ）
2. これで、工程マスタを編集するたびに自動で「KBL 計画」カレンダーへ反映されるようになる

## 4. 動作確認

- 工程マスタの予定日を変更・追加すると、「KBL 計画」カレンダーにその日の終日予定として反映される
- 職人さんが日報を送ると、「KBL 実績」カレンダーにその日の終日予定として反映される
- Googleカレンダーで両方のカレンダーを同時に表示すれば、計画と実績を重ねて見比べられます
- 通知が届かない場合は、まずWorkerのログ（Cloudflareダッシュボードの「ログ」タブ）でエラーが出ていないか確認してください
